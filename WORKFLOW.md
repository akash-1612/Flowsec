# 🎯 Flowsec Deployment Workflow

This document provides a visual overview of the deployment process.

## 📊 Complete Workflow Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                    FLOWSEC DEPLOYMENT WORKFLOW                   │
└─────────────────────────────────────────────────────────────────┘

STEP 1: FORK & CLONE
┌──────────────────────────────────────────────────────────────┐
│  Teammate's GitHub → Fork → Your GitHub → Clone → Local PC  │
│  [Original Repo]      [Fork]  [Your Repo]   [git clone]      │
└──────────────────────────────────────────────────────────────┘
                            ↓
STEP 2: CREATE SUPABASE PROJECT
┌──────────────────────────────────────────────────────────────┐
│  Supabase.com → New Project → Copy URL & Keys               │
│  • Project URL: https://xxx.supabase.co                       │
│  • Anon Key: eyJ...                                           │
└──────────────────────────────────────────────────────────────┘
                            ↓
STEP 3: SET UP DATABASE
┌──────────────────────────────────────────────────────────────┐
│  Supabase Dashboard → SQL Editor → Run database_schema.sql   │
│  ✓ Tables: profiles, messages, files, backup_keys            │
│  ✓ RLS Policies: Enabled on all tables                       │
│  ✓ Indexes: Created for performance                          │
└──────────────────────────────────────────────────────────────┘
                            ↓
STEP 4: CONFIGURE AUTHENTICATION
┌──────────────────────────────────────────────────────────────┐
│  Supabase → Authentication → Settings                         │
│  ✓ Enable Email Provider                                      │
│  ✓ Add Redirect URLs:                                         │
│    - https://YOUR_USERNAME.github.io/Flowsec/pages/chat.html │
│    - https://YOUR_USERNAME.github.io/Flowsec/pages/otp.html  │
└──────────────────────────────────────────────────────────────┘
                            ↓
STEP 5: CONFIGURE APPLICATION
┌──────────────────────────────────────────────────────────────┐
│  Option A: Run setup.bat (Automated)                         │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │  • Generates encryption key                             │ │
│  │  • Updates js/config.js                                 │ │
│  │  • Commits changes                                      │ │
│  └─────────────────────────────────────────────────────────┘ │
│                           OR                                  │
│  Option B: Manual Configuration                              │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │  1. Generate key: openssl rand -base64 32              │ │
│  │  2. Edit js/config.js:                                  │ │
│  │     - SUPABASE_URL = 'your-url'                         │ │
│  │     - SUPABASE_KEY = 'your-key'                         │ │
│  │     - SUPABASE_APP_KEY = 'generated-key'                │ │
│  └─────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
                            ↓
STEP 6: COMMIT & PUSH TO GITHUB
┌──────────────────────────────────────────────────────────────┐
│  git add .                                                    │
│  git commit -m "Configure for my Supabase account"           │
│  git push origin main                                         │
└──────────────────────────────────────────────────────────────┘
                            ↓
STEP 7: ENABLE GITHUB PAGES
┌──────────────────────────────────────────────────────────────┐
│  GitHub Repo → Settings → Pages                              │
│  Source: GitHub Actions                                       │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  GitHub Actions Workflow (.github/workflows/deploy.yml) │ │
│  │  ↓                                                       │ │
│  │  • Checkout code                                        │ │
│  │  • Setup Pages                                          │ │
│  │  • Upload artifact                                      │ │
│  │  • Deploy to Pages                                      │ │
│  └────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
                            ↓
STEP 8: DEPLOYMENT COMPLETE! 🎉
┌──────────────────────────────────────────────────────────────┐
│  Your live URL:                                               │
│  https://YOUR_USERNAME.github.io/Flowsec/                    │
│                                                               │
│  Test the app:                                                │
│  ✓ Sign up with email                                        │
│  ✓ Verify OTP code                                           │
│  ✓ Complete profile                                          │
│  ✓ Send messages                                             │
│  ✓ Upload files                                              │
└──────────────────────────────────────────────────────────────┘
```

## 🏗️ Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                        USER'S BROWSER                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │  HTML Pages  │  │  JavaScript  │  │  CSS Styles  │         │
│  │  • login     │  │  • auth.js   │  │  • auth.css  │         │
│  │  • signup    │  │  • chat.js   │  │  • chat.css  │         │
│  │  • chat      │  │  • encryption│  │  • ...       │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
└─────────────────────────────────────────────────────────────────┘
                            ↓ ↑
                    (HTTPS/WebSocket)
                            ↓ ↑
┌─────────────────────────────────────────────────────────────────┐
│                      GITHUB PAGES                                │
│  • Serves static files (HTML, CSS, JS)                          │
│  • URL: https://YOUR_USERNAME.github.io/Flowsec/                │
│  • Deployed via GitHub Actions                                  │
└─────────────────────────────────────────────────────────────────┘
                            ↓ ↑
                    (API Calls - REST/WebSocket)
                            ↓ ↑
┌─────────────────────────────────────────────────────────────────┐
│                         SUPABASE                                 │
│  ┌───────────────┐  ┌───────────────┐  ┌────────────────┐     │
│  │  PostgreSQL   │  │  Auth Service │  │  Storage       │     │
│  │  • profiles   │  │  • OTP login  │  │  • user-files  │     │
│  │  • messages   │  │  • Sessions   │  │  • Encrypted   │     │
│  │  • files      │  │  • JWT tokens │  │    files       │     │
│  │  • backup_keys│  └───────────────┘  └────────────────┘     │
│  └───────────────┘                                              │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Edge Functions (Optional)                                │  │
│  │  • encrypt - Server-side encryption                       │  │
│  │  • decrypt - Server-side decryption                       │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                            ↓ ↑
                    (API Calls - Optional)
                            ↓ ↑
┌─────────────────────────────────────────────────────────────────┐
│                    EXTERNAL SERVICES                             │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  VirusTotal API (Optional)                                 │ │
│  │  • File scanning                                           │ │
│  │  • Malware detection                                       │ │
│  └───────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

## 📊 Data Flow for Sending a Message

```
┌──────────────────────────────────────────────────────────────────┐
│                   SENDING AN ENCRYPTED MESSAGE                    │
└──────────────────────────────────────────────────────────────────┘

User Types Message
      ↓
┌─────────────────────────────────────────────┐
│ 1. Client-Side Encryption (encryption.js)   │
│    • Generate random IV (Initialization Vector)
│    • Use user's encryption key               │
│    • Encrypt with AES-GCM                    │
│    • Result: { encrypted_content, iv, salt } │
└─────────────────────────────────────────────┘
      ↓
┌─────────────────────────────────────────────┐
│ 2. Send to Supabase (chat.js)               │
│    • Call supabaseClient.from('messages')    │
│    • Insert encrypted data                   │
│    • Include sender_id, receiver_id          │
└─────────────────────────────────────────────┘
      ↓
┌─────────────────────────────────────────────┐
│ 3. Supabase Processing                       │
│    • Validate RLS policies                   │
│    • Store in PostgreSQL                     │
│    • Trigger real-time notifications         │
└─────────────────────────────────────────────┘
      ↓
┌─────────────────────────────────────────────┐
│ 4. Receiver's Client                         │
│    • Receive real-time update                │
│    • Fetch encrypted message                 │
│    • Decrypt with receiver's key             │
│    • Display plain text                      │
└─────────────────────────────────────────────┘
```

## 🔐 Security Layers

```
┌────────────────────────────────────────────────────────────┐
│                    SECURITY ARCHITECTURE                    │
└────────────────────────────────────────────────────────────┘

Layer 1: TRANSPORT SECURITY
├─ HTTPS (TLS 1.3)
├─ Secure WebSocket (WSS)
└─ Certificate validation

Layer 2: AUTHENTICATION
├─ Passwordless OTP
├─ JWT tokens
├─ Session management
└─ Multi-account isolation

Layer 3: AUTHORIZATION (Row Level Security)
├─ User can only access own profile
├─ User can only see own messages
├─ User can only access own/shared files
└─ Automatic policy enforcement

Layer 4: ENCRYPTION
├─ End-to-End Encryption (AES-GCM)
├─ Client-side key generation
├─ Unique IV per message
└─ Key backup & recovery

Layer 5: FILE SECURITY
├─ VirusTotal scanning
├─ Encrypted file storage
├─ Access control lists
└─ Automatic virus detection

Layer 6: DATABASE SECURITY
├─ Parameterized queries (SQL injection prevention)
├─ RLS policies
├─ Encrypted backups
└─ Audit logs
```

## 🔄 Update Workflow (from Upstream)

```
When your teammate makes updates:

Teammate's Repo (upstream)
      ↓ (they push changes)
GitHub
      ↓ (you fetch)
┌─────────────────────────────────┐
│ Your Local Machine              │
│ $ git fetch upstream            │
│ $ git merge upstream/main       │
│ $ git push origin main          │
└─────────────────────────────────┘
      ↓ (push to your fork)
Your GitHub Repo
      ↓ (GitHub Actions triggered)
GitHub Pages (Auto-Deploy)
```

## 📱 User Journey

```
New User Journey:
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│  Visit   │ → │  Sign Up │ → │  Get OTP │ → │  Verify  │
│   Site   │    │   Page   │    │  by Email│    │   OTP    │
└──────────┘    └──────────┘    └──────────┘    └──────────┘
                                                      ↓
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│   Chat   │ ← │   Send   │ ← │   Find   │ ← │ Complete │
│   View   │    │ Messages │    │  Users   │    │ Profile  │
└──────────┘    └──────────┘    └──────────┘    └──────────┘

Returning User Journey:
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│  Visit   │ → │  Login   │ → │  Get OTP │ → │  Start   │
│   Site   │    │   Page   │    │  by Email│    │ Chatting │
└──────────┘    └──────────┘    └──────────┘    └──────────┘
```

## 📊 File Structure Overview

```
Flowsec/
│
├── 📄 Setup & Documentation
│   ├── README.md                    ← Project overview
│   ├── SETUP_GUIDE.md              ← Detailed setup guide
│   ├── DEPLOYMENT_CHECKLIST.md     ← Quick checklist
│   ├── GET_STARTED.md              ← Quick start guide
│   ├── WORKFLOW.md                 ← This file
│   ├── setup.ps1                   ← PowerShell setup script
│   └── setup.bat                   ← Batch file wrapper
│
├── 🗄️ Database & Storage
│   ├── database_schema.sql         ← Main database setup
│   └── storage_setup.sql           ← Storage bucket setup
│
├── 🌐 Frontend
│   ├── index.html                  ← Landing page
│   ├── pages/                      ← Application pages
│   │   ├── login.html
│   │   ├── signup.html
│   │   ├── otp.html
│   │   ├── chat.html
│   │   └── ...
│   ├── js/                         ← JavaScript modules
│   │   ├── config.js              ← Configuration (EDIT THIS!)
│   │   ├── auth.js
│   │   ├── chat.js
│   │   ├── encryption.js
│   │   └── ...
│   └── css/                        ← Stylesheets
│
├── ⚡ Backend (Supabase)
│   └── supabase/
│       └── functions/              ← Edge functions
│           ├── encrypt/
│           └── decrypt/
│
└── 🚀 Deployment
    └── .github/
        └── workflows/
            └── deploy.yml          ← GitHub Actions workflow
```

## ⏱️ Estimated Time for Each Step

```
┌─────────────────────────────────────────┬──────────────┐
│ Step                                     │ Time         │
├─────────────────────────────────────────┼──────────────┤
│ Fork & Clone Repository                 │ 2-3 minutes  │
│ Create Supabase Project                 │ 3-5 minutes  │
│ Set Up Database (run SQL script)        │ 1-2 minutes  │
│ Configure Authentication                │ 2-3 minutes  │
│ Configure Application (run setup.bat)   │ 2-3 minutes  │
│ Commit & Push to GitHub                 │ 1-2 minutes  │
│ Enable GitHub Pages                     │ 1 minute     │
│ Wait for Deployment                     │ 2-5 minutes  │
│ Test the Application                    │ 3-5 minutes  │
├─────────────────────────────────────────┼──────────────┤
│ TOTAL TIME                               │ 20-30 min    │
└─────────────────────────────────────────┴──────────────┘
```

## 🎯 Success Indicators

After completing all steps, you should be able to:

✅ Access your site at `https://YOUR_USERNAME.github.io/Flowsec/`
✅ Sign up with your email address
✅ Receive an OTP code via email
✅ Complete your profile with name and bio
✅ See the chat interface
✅ Send and receive encrypted messages
✅ Upload files (if storage is configured)
✅ Switch between light and dark themes

## 🆘 Quick Troubleshooting Guide

```
Issue: Can't access GitHub Pages
├─ Wait 5-10 minutes after first deployment
├─ Check Actions tab for errors
└─ Verify Pages is enabled in Settings

Issue: Can't log in
├─ Check browser console (F12)
├─ Verify Supabase URL and key in config.js
├─ Ensure email auth is enabled in Supabase
└─ Check spam folder for OTP email

Issue: Database errors
├─ Verify database_schema.sql was executed
├─ Check all tables exist in Supabase Table Editor
├─ Ensure RLS policies are enabled
└─ Verify you're authenticated

Issue: File uploads fail
├─ Check if storage bucket 'user-files' exists
├─ Verify storage policies are set up
├─ Check file size (max 50MB on free tier)
└─ Review browser console for errors
```

---

**Ready to deploy?** Start with `setup.bat` or follow [SETUP_GUIDE.md](SETUP_GUIDE.md)!
